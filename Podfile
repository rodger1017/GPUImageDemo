platform :ios, '12.0'
install! 'cocoapods', :disable_input_output_paths => true, :warn_for_unused_master_specs_repo => false,:deterministic_uuids => false

target 'FilterShowcase' do
  pod 'Masonry'
  pod 'GPUImage', :git => 'https://github.com/rodger1017/GPUImage.git'
end
